import { MODAL_WINDOW_ACTIONS } from '../action-types/modalWindowActionTypes';

export const closeWindow = {
  type: MODAL_WINDOW_ACTIONS.CLOSE
}

export const contentChange = {
  type: MODAL_WINDOW_ACTIONS.CONTENT_CHANGE
}
